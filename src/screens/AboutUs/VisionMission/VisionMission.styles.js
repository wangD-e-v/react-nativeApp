import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        flex:1,
    },
    coverLogo:{
        width:'100%',
        height:100,
        resizeMode:'contain',
        marginTop:20,
        marginBottom:20
    },
    visionMission:{
        width:'100%',
        height:500,
        resizeMode:'cover'
    }
})

export default styles;