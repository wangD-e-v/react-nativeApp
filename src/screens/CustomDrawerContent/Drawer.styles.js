import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    drawerContent:{
        flex:1,
        marginTop:30,
    },
    drawerImage:{
        width:'100%',
        height:100,
        resizeMode:'contain'
    }
})

export default styles;